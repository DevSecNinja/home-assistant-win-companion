# Template entities

Template entities derive new Home Assistant state from sensors that Windows
Companion already reports. They do not add client traffic.

| Example | Purpose |
| --- | --- |
| [Windows PC connectivity](device-connectivity/) | Mark a PC disconnected when Home Assistant has not received a recent sensor report |

Each template has its own directory containing installation guidance and the
configuration artifact.
