---
extends: default

rules:
  comments: disable
  line-length: disable
  document-start: disable
  truthy:
    allowed-values:
      - "true"
      - "false"
    check-keys: false

ignore: |-
  .specify/
  apm.yml
  apm.lock.yaml
  apm_modules/
