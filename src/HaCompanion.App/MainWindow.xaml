<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="HaCompanion_App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:HaCompanion_App"
    xmlns:tb="using:H.NotifyIcon"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Home Assistant Companion"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="Home Assistant Companion">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/AppIcon.ico" />
            </TitleBar.IconSource>
        </TitleBar>

        <tb:TaskbarIcon
            x:Name="TrayIcon"
            ToolTipText="Home Assistant Companion"
            IconSource="Assets/AppIcon.ico">
            <tb:TaskbarIcon.ContextFlyout>
                <MenuFlyout>
                    <MenuFlyoutItem Text="Open Home Assistant" Click="OnOpenHomeAssistant" />
                    <MenuFlyoutItem Text="Show Companion" Click="OnShowWindow" />
                    <MenuFlyoutSeparator />
                    <MenuFlyoutItem Text="Disconnect" Click="OnDisconnect" />
                    <MenuFlyoutItem Text="Exit" Click="OnExit" />
                </MenuFlyout>
            </tb:TaskbarIcon.ContextFlyout>
        </tb:TaskbarIcon>

        <!-- Connect view -->
        <StackPanel
            x:Name="ConnectPanel"
            Grid.Row="1"
            Width="380"
            Padding="24"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Spacing="12">
            <TextBlock Text="Connect to Home Assistant" Style="{StaticResource SubtitleTextBlockStyle}" />
            <TextBlock
                Text="Enter your Home Assistant URL. You'll sign in through your browser — no tokens to copy."
                TextWrapping="Wrap"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <TextBox
                x:Name="UrlBox"
                Header="Home Assistant URL"
                PlaceholderText="http://homeassistant.local:8123" />
            <Button
                x:Name="SignInButton"
                Content="Sign in"
                Style="{StaticResource AccentButtonStyle}"
                Click="OnSignIn" />
            <ProgressRing x:Name="SignInProgress" IsActive="False" Width="24" Height="24" HorizontalAlignment="Left" />
            <TextBlock
                x:Name="ConnectError"
                Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                TextWrapping="Wrap"
                Visibility="Collapsed" />
        </StackPanel>

        <!-- Status view -->
        <StackPanel
            x:Name="StatusPanel"
            Grid.Row="1"
            Width="380"
            Padding="24"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Spacing="12"
            Visibility="Collapsed">
            <TextBlock Text="Home Assistant Companion" Style="{StaticResource SubtitleTextBlockStyle}" />
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock Text="Server:" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <TextBlock x:Name="ServerText" Text="—" TextTrimming="CharacterEllipsis" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock Text="Status:" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <TextBlock x:Name="StatusText" Text="Connecting…" FontWeight="SemiBold" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock Text="Health:" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <TextBlock x:Name="HealthText" Text="—" FontWeight="SemiBold" TextWrapping="Wrap" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock Text="Battery:" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <TextBlock x:Name="BatteryText" Text="—" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock Text="Last update:" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <TextBlock x:Name="LastUpdateText" Text="—" />
            </StackPanel>
            <Button Content="Open Home Assistant" Click="OnOpenHomeAssistant" HorizontalAlignment="Stretch" />
            <Button Content="Change server URL…" Click="OnChangeServerUrl" HorizontalAlignment="Stretch" />
            <Button x:Name="UpdateNowButton" Content="Update now" Click="OnForcePush" HorizontalAlignment="Stretch" />
            <Button Content="Sensors…" Click="OnShowSettings" HorizontalAlignment="Stretch" />
            <Button x:Name="DisconnectButton" Content="Disconnect" Click="OnDisconnect" HorizontalAlignment="Stretch" />
            <Button Content="Open log file" Click="OnOpenLog" HorizontalAlignment="Stretch" />
            <Button
                Content="Remove server…"
                Click="OnRemoveServer"
                HorizontalAlignment="Stretch"
                Foreground="{ThemeResource SystemFillColorCriticalBrush}" />
        </StackPanel>

        <!-- Sensor settings view -->
        <Grid x:Name="SettingsPanel" Grid.Row="1" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Padding="24,16,24,8" Spacing="4">
                <TextBlock Text="Sensors" Style="{StaticResource SubtitleTextBlockStyle}" />
                <TextBlock
                    Text="Choose what this PC reports to Home Assistant. Anything switched off is never collected or sent."
                    TextWrapping="Wrap"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <ScrollViewer Grid.Row="1" Padding="24,0,24,0">
                <StackPanel x:Name="SensorList" Spacing="4" />
            </ScrollViewer>

            <StackPanel Grid.Row="2" Padding="24,8,24,20" Spacing="12">
                <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                    <TextBlock Text="Idle after" VerticalAlignment="Center" />
                    <NumberBox
                        x:Name="IdleMinutesBox"
                        Minimum="1"
                        Maximum="240"
                        SpinButtonPlacementMode="Compact"
                        Width="120"
                        ValueChanged="OnIdleMinutesChanged" />
                    <TextBlock Text="minutes without input" VerticalAlignment="Center"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </StackPanel>
                <Button
                    Content="Open Windows Location settings"
                    Click="OnOpenLocationSettings"
                    HorizontalAlignment="Stretch" />
                <StackPanel Spacing="4">
                    <TextBlock Text="Frontmost app detail" />
                    <ComboBox
                        x:Name="FrontmostAppModeBox"
                        HorizontalAlignment="Stretch"
                        SelectionChanged="OnFrontmostAppModeChanged">
                        <ComboBoxItem Content="Application name only" Tag="ApplicationName" />
                        <ComboBoxItem Content="Full window title" Tag="FullWindowTitle" />
                    </ComboBox>
                    <TextBlock
                        Text="Full titles may reveal document names, messages, customer names and websites."
                        TextWrapping="Wrap"
                        FontSize="12"
                        Foreground="{ThemeResource SystemFillColorCautionBrush}" />
                </StackPanel>
                <Button Content="Back" Click="OnCloseSettings" HorizontalAlignment="Stretch" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
